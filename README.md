Repo for GEOM184 ShinyApp

Important: please make sure the "heatmap.tif" is properly downloaded, sometimes it doesn't copy over properly when using git clone. It may need to be downloaded manually.

Also make sure to set the current working directory in R studio to to source file location by going to "Session -> Set Working Directory -> To Source File Location".
